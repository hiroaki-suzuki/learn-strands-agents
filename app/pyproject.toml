[project]
name = "learn-strands-agents"
version = "0.1.0"
description = "learn strands agents"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ruff>=0.14.13",
    "ty>=0.0.12",
]
